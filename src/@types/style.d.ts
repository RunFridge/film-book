export interface Theme {
  enabled: string;
  disabled: string;
  primary: string;
  bgPrimary: string;
  bgSecondary: string;
  navColor: string;
}

export type MobileMenuItemType = {
  current: boolean;
};

import React, { ReactElement } from "react";
import styled from "styled-components";

const Wrapper = styled.footer``;

const Footer = (): ReactElement => <Wrapper>Footer</Wrapper>;

export default Footer;
